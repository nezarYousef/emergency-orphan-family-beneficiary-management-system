@extends('layouts.app')
@section('content')
<div class="page-heading"><div><p class="eyebrow"><span class="eyebrow-line"></span> People served</p><h1>Beneficiaries</h1><p>Find individuals connected to a family case.</p></div><div class="d-flex flex-wrap gap-2">@can('export-data')<a class="btn btn-outline-primary" href="{{ route('export', ['type' => 'beneficiaries'] + request()->query()) }}">Export CSV</a>@endcan @can('create-records')<a class="btn btn-primary" href="/beneficiaries/create">Add beneficiary <span aria-hidden="true">+</span></a>@endcan</div></div>
<form class="filter-panel row g-3 mb-4" method="get" aria-label="Filter beneficiaries"><div class="col-lg-6"><label class="form-label" for="beneficiary-search">Search</label><input class="form-control" id="beneficiary-search" name="search" value="{{ request('search') }}" placeholder="Number, name, national ID"></div><div class="col-md-4 col-lg-2"><label class="form-label" for="beneficiary-type">Type</label><select class="form-select" id="beneficiary-type" name="beneficiary_type"><option value="">All types</option>@foreach(['child','adult','elderly','person_with_disability','caregiver','other'] as $value)<option value="{{ $value }}" @selected(request('beneficiary_type') === $value)>{{ str($value)->replace('_',' ')->title() }}</option>@endforeach</select></div><div class="col-md-4 col-lg-2"><label class="form-label" for="beneficiary-gender">Gender</label><select class="form-select" id="beneficiary-gender" name="gender"><option value="">All</option><option value="female" @selected(request('gender') === 'female')>Female</option><option value="male" @selected(request('gender') === 'male')>Male</option></select></div><div class="col-md-4 col-lg-2 d-flex align-items-end gap-2"><button class="btn btn-primary flex-grow-1" type="submit">Filter</button><a class="btn btn-outline-secondary" href="{{ route('beneficiaries.index') }}" aria-label="Clear filters">×</a></div></form>
<div class="table-card card"><div class="table-responsive"><table class="table"><caption class="visually-hidden">Beneficiary records</caption><thead><tr><th scope="col">Number</th><th scope="col">Name</th><th scope="col">Family</th><th scope="col">Gender</th><th scope="col">Age</th><th scope="col">Type</th><th scope="col">Disability</th></tr></thead><tbody>@forelse($beneficiaries as $beneficiary)<tr><td><a href="{{ route('beneficiaries.show', $beneficiary) }}">{{ $beneficiary->beneficiary_number }}</a></td><td>{{ $beneficiary->full_name }}</td><td>{{ $beneficiary->family?->case_number }}</td><td>{{ str($beneficiary->gender)->title() }}</td><td>{{ $beneficiary->date_of_birth?->age }}</td><td><span class="badge text-bg-light">{{ str($beneficiary->beneficiary_type)->replace('_',' ')->title() }}</span></td><td>{{ $beneficiary->disability_status ? 'Yes' : 'No' }}</td></tr>@empty<tr><td colspan="7"><div class="empty-state"><div class="empty-state-icon">♧</div><h2 class="h6">No beneficiaries found</h2><p class="mb-0">Try clearing the filters or add a new record.</p></div></td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $beneficiaries->links() }}</div>
@endsection
