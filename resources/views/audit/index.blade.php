@extends('layouts.app')
@section('content')
<div class="page-heading"><div><p class="eyebrow"><span class="eyebrow-line"></span> Accountability</p><h1>Audit logs</h1><p>Admin-only history of sensitive record changes.</p></div></div>
<form class="filter-panel row g-3 mb-4" method="get" aria-label="Filter audit logs"><div class="col-lg-8"><label class="form-label" for="audit-search">Search</label><input class="form-control" id="audit-search" name="search" value="{{ request('search') }}" placeholder="Action, model, description"></div><div class="col-md-4 d-flex align-items-end gap-2"><button class="btn btn-primary flex-grow-1" type="submit">Search</button><a class="btn btn-outline-secondary" href="{{ route('audit.index') }}" aria-label="Clear filters">×</a></div></form>
<div class="table-card card"><div class="table-responsive"><table class="table"><caption class="visually-hidden">Audit log entries</caption><thead><tr><th scope="col">Time</th><th scope="col">User</th><th scope="col">Action</th><th scope="col">Model</th><th scope="col">Description</th><th scope="col">IP</th></tr></thead><tbody>@forelse($logs as $log)<tr><td>{{ $log->created_at?->format('Y-m-d H:i') }}</td><td>{{ $log->user?->email ?? 'System' }}</td><td><span class="badge text-bg-light">{{ str($log->action)->title() }}</span></td><td>{{ $log->model_type }} #{{ $log->model_id }}</td><td>{{ $log->description }}</td><td>{{ $log->ip_address }}</td></tr>@empty<tr><td colspan="6"><div class="empty-state"><div class="empty-state-icon">⌁</div><h2 class="h6">No audit entries found</h2><p class="mb-0">Activity will appear here as records change.</p></div></td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $logs->links() }}</div>
@endsection
