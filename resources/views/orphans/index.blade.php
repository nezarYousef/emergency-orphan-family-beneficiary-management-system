@extends('layouts.app')
@section('content')
<div class="page-heading"><div><p class="eyebrow"><span class="eyebrow-line"></span> Priority cases</p><h1>Orphans</h1><p>Track support, guardianship, and sponsorship follow-up.</p></div><div class="d-flex flex-wrap gap-2">@can('export-data')<a class="btn btn-outline-primary" href="{{ route('export', ['type' => 'orphans'] + request()->query()) }}">Export CSV</a>@endcan @can('create-records')<a class="btn btn-primary" href="/orphans/create">Add orphan record <span aria-hidden="true">+</span></a>@endcan</div></div>
<form class="filter-panel row g-3 mb-4" method="get" aria-label="Filter orphan records"><div class="col-lg-8"><label class="form-label" for="orphan-search">Search</label><input class="form-control" id="orphan-search" name="search" value="{{ request('search') }}" placeholder="Orphan number, beneficiary, guardian"></div><div class="col-md-4 d-flex align-items-end gap-2"><button class="btn btn-primary flex-grow-1" type="submit">Search</button><a class="btn btn-outline-secondary" href="{{ route('orphans.index') }}" aria-label="Clear filters">×</a></div></form>
<div class="table-card card"><div class="table-responsive"><table class="table"><caption class="visually-hidden">Orphan records</caption><thead><tr><th scope="col">Number</th><th scope="col">Beneficiary</th><th scope="col">Family</th><th scope="col">Status</th><th scope="col">Sponsorship</th></tr></thead><tbody>@forelse($orphans as $orphan)<tr><td><a href="{{ route('orphans.show', $orphan) }}">{{ $orphan->orphan_number }}</a></td><td>{{ $orphan->beneficiary?->full_name }}</td><td>{{ $orphan->family?->case_number }}</td><td><span class="badge text-bg-warning">{{ str($orphan->orphan_status)->replace('_',' ')->title() }}</span></td><td>{{ str($orphan->sponsorship_status)->replace('_',' ')->title() }}</td></tr>@empty<tr><td colspan="5"><div class="empty-state"><div class="empty-state-icon">♡</div><h2 class="h6">No orphan records found</h2><p class="mb-0">Try clearing the search or add a priority case.</p></div></td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $orphans->links() }}</div>
@endsection
