@extends('layouts.app')
@section('content')
<div class="page-heading"><div><p class="eyebrow"><span class="eyebrow-line"></span> Assistance ledger</p><h1>Aid distributions</h1><p>Review support delivered to families and beneficiaries.</p></div><div class="d-flex flex-wrap gap-2"><a class="btn btn-outline-primary" href="{{ route('export', ['type' => 'aid'] + request()->query()) }}">Export CSV</a>@if(in_array(auth()->user()->role, ['admin','data_entry'], true))<a class="btn btn-primary" href="/aid-distributions/create">Record aid <span aria-hidden="true">+</span></a>@endif</div></div>
<form class="filter-panel row g-3 mb-4" method="get" aria-label="Filter aid distributions"><div class="col-lg-8"><label class="form-label" for="aid-search">Search</label><input class="form-control" id="aid-search" name="search" value="{{ request('search') }}" placeholder="Aid type, reference, family case"></div><div class="col-md-4 d-flex align-items-end gap-2"><button class="btn btn-primary flex-grow-1" type="submit">Search</button><a class="btn btn-outline-secondary" href="{{ route('aid.index') }}" aria-label="Clear filters">×</a></div></form>
<div class="table-card card"><div class="table-responsive"><table class="table"><caption class="visually-hidden">Aid distribution records</caption><thead><tr><th scope="col">Date</th><th scope="col">Family</th><th scope="col">Beneficiary</th><th scope="col">Type</th><th scope="col">Amount</th><th scope="col">Reference</th></tr></thead><tbody>@forelse($distributions as $distribution)<tr><td><a href="{{ route('aid.show', $distribution) }}">{{ $distribution->distribution_date?->format('Y-m-d') }}</a></td><td>{{ $distribution->family?->case_number }}</td><td>{{ $distribution->beneficiary?->full_name ?? 'Family-level' }}</td><td>{{ str($distribution->aid_type)->title() }}</td><td>{{ $distribution->amount ?? '—' }} {{ $distribution->currency }}</td><td>{{ $distribution->reference_number ?? '—' }}</td></tr>@empty<tr><td colspan="6"><div class="empty-state"><div class="empty-state-icon">▣</div><h2 class="h6">No aid distributions found</h2><p class="mb-0">Try clearing the search or record a distribution.</p></div></td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $distributions->links() }}</div>
@endsection
