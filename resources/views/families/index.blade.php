@extends('layouts.app')
@section('content')
<div class="page-heading"><div><p class="eyebrow"><span class="eyebrow-line"></span> Case registry</p><h1>Families</h1><p>Search and manage household-level case records.</p></div><div class="d-flex flex-wrap gap-2"> @can('export-data')<a class="btn btn-outline-primary" href="{{ route('export', ['type' => 'families'] + request()->query()) }}">Export CSV</a>@endcan @can('create-records')<a class="btn btn-primary" href="/families/create">Add family <span aria-hidden="true">+</span></a>@endcan</div></div>
<form class="filter-panel row g-3 mb-4" method="get" aria-label="Filter families"><div class="col-lg-5"><label class="form-label" for="family-search">Search</label><input class="form-control" id="family-search" name="search" value="{{ request('search') }}" placeholder="Case, household, ID, phone"></div><div class="col-md-4 col-lg-3"><label class="form-label" for="family-region">Governorate</label><select class="form-select" id="family-region" name="governorate"><option value="">All governorates</option>@foreach(['North Gaza','Gaza','Deir al-Balah','Khan Younis','Rafah'] as $value)<option value="{{ $value }}" @selected(request('governorate') === $value)>{{ $value }}</option>@endforeach</select></div><div class="col-md-4 col-lg-2"><label class="form-label" for="family-provider">Provider status</label><select class="form-select" id="family-provider" name="provider_status"><option value="">All statuses</option>@foreach(['has_provider','no_provider','deceased_provider','missing_provider','disabled_provider'] as $value)<option value="{{ $value }}" @selected(request('provider_status') === $value)>{{ str($value)->replace('_',' ')->title() }}</option>@endforeach</select></div><div class="col-md-4 col-lg-2 d-flex align-items-end gap-2"><button class="btn btn-primary flex-grow-1" type="submit">Filter</button><a class="btn btn-outline-secondary" href="{{ route('families.index') }}" aria-label="Clear filters">×</a></div></form>
<div class="table-card card"><div class="table-responsive"><table class="table"><caption class="visually-hidden">Family case records</caption><thead><tr><th scope="col">Case</th><th scope="col">Head of household</th><th scope="col">Governorate</th><th scope="col">Area</th><th scope="col">Size</th><th scope="col">Beneficiaries</th><th scope="col">Vulnerability</th><th scope="col"><span class="visually-hidden">Actions</span></th></tr></thead><tbody>@forelse($families as $family)<tr><td><a href="{{ route('families.show', $family) }}">{{ $family->case_number }}</a></td><td>{{ $family->head_of_household_name }}</td><td>{{ $family->governorate }}</td><td>{{ $family->area }}</td><td>{{ $family->family_size }}</td><td>{{ $family->beneficiaries_count }}</td><td><span class="badge text-bg-warning">{{ str($family->vulnerability_status)->title() }}</span></td><td>@can('edit-records')<a href="{{ route('families.edit', $family) }}">Edit</a>@else<span class="text-muted small">View</span>@endcan</td></tr>@empty<tr><td colspan="8"><div class="empty-state"><div class="empty-state-icon">◌</div><h2 class="h6">No families found</h2><p class="mb-0">Try clearing the filters or add a new case.</p></div></td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $families->links() }}</div>
@endsection
