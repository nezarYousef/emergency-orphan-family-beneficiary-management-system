@extends('layouts.app')
@section('content')
<h1 class="mb-4">Management Dashboard</h1><div class="row g-3 mb-4">@foreach(['Families'=>$families,'Beneficiaries'=>$beneficiaries,'Orphans'=>$orphans,'Families Without Provider'=>$withoutProvider] as $label=>$value)<div class="col-md-3"><div class="card shadow-sm"><div class="card-body"><div class="text-muted">{{$label}}</div><div class="display-6">{{$value}}</div></div></div></div>@endforeach</div><div class="row g-4"><div class="col-lg-6"><div class="card"><div class="card-header">Recent Families</div>@foreach($recentFamilies as $f)<div class="list-group-item p-2">{{$f->case_number}} — {{$f->head_of_household_name}}</div>@endforeach</div></div><div class="col-lg-6"><div class="card"><div class="card-header">Recent Aid</div>@foreach($recentAid as $a)<div class="list-group-item p-2">{{$a->distribution_date?->format('Y-m-d')}} — {{$a->aid_type}}</div>@endforeach</div></div></div>
@endsection
