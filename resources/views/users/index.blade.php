@extends('layouts.app')
@section('content')
<div class="page-heading"><div><p class="eyebrow"><span class="eyebrow-line"></span> Access control</p><h1>Users</h1><p>Manage workspace access and account status.</p></div><a class="btn btn-primary" href="{{ route('users.create') }}">Add user <span aria-hidden="true">+</span></a></div>
<form class="filter-panel row g-3 mb-4" method="get" aria-label="Search users"><div class="col-lg-8"><label class="form-label" for="user-search">Search</label><input class="form-control" id="user-search" name="search" value="{{ request('search') }}" placeholder="Name or email"></div><div class="col-md-4 d-flex align-items-end gap-2"><button class="btn btn-primary flex-grow-1" type="submit">Search</button><a class="btn btn-outline-secondary" href="{{ route('users.index') }}" aria-label="Clear search">×</a></div></form>
<div class="table-card card"><div class="table-responsive"><table class="table"><caption class="visually-hidden">Workspace users</caption><thead><tr><th scope="col">Name</th><th scope="col">Email</th><th scope="col">Role</th><th scope="col">Status</th><th scope="col"><span class="visually-hidden">Actions</span></th></tr></thead><tbody>@forelse($users as $user)<tr><td><strong>{{ $user->name }}</strong></td><td>{{ $user->email }}</td><td><span class="badge text-bg-light">{{ str($user->role)->replace('_',' ')->title() }}</span></td><td><span class="badge {{ $user->is_active ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $user->is_active ? 'Active' : 'Inactive' }}</span></td><td><a href="{{ route('users.edit', $user) }}">Edit</a>@if($user->is_active && $user->id !== auth()->id())<form class="d-inline ms-2" method="post" action="{{ route('users.destroy', $user) }}" data-no-loading>@csrf @method('DELETE')<button class="btn btn-link btn-sm p-0 text-danger" type="submit" onclick="return confirm('Deactivate this user?')">Deactivate</button></form>@endif</td></tr>@empty<tr><td colspan="5"><div class="empty-state"><div class="empty-state-icon">♙</div><h2 class="h6">No users found</h2><p class="mb-0">Add a workspace user to get started.</p></div></td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $users->links() }}</div>
@endsection
